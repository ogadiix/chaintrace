[project]
name = "chaintrace-shared"
version = "0.1.0"
description = "ChainTrace Shared Python Types & Constants"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
